# ============================================================
# Variáveis de ambiente — copie este arquivo para .env.local
# e preencha com os valores reais (nunca commite o .env.local)
# ============================================================

# URL do backend (em desenvolvimento local)
NEXT_PUBLIC_API_URL=http://localhost:8080

